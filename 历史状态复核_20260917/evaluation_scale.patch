--- before/归一.py
+++ after/归一.py
@@ -10,6 +10,7 @@
     f1_score, roc_auc_score, mean_squared_error,
     mean_absolute_percentage_error
 )
+from regression_scale_utils import regression_metrics_original_price
 from sklearn.ensemble import (
     GradientBoostingClassifier, GradientBoostingRegressor,
     RandomForestClassifier, RandomForestRegressor
@@ -154,7 +155,8 @@
 
 # Define MAPE (Mean Absolute Percentage Error)
 def mean_absolute_percentage_error_custom(y_true, y_pred):
-    return mean_absolute_percentage_error(y_true, y_pred)
+    """Return MAPE in percent; 2.5 means 2.5%."""
+    return mean_absolute_percentage_error(y_true, y_pred) * 100.0
 
 # Define data preprocessing function
 def preprocess_data(file_path):
@@ -1017,20 +1019,21 @@
 
         # Predict with Meta-Model for Regression
         fold_meta_preds_reg_scaled = meta_model_reg.predict(fold_meta_features_reg)
-        # Inverse transform the predictions to original scale (if needed)
-        # fold_meta_preds_reg = scaler_y_reg_eval.inverse_transform(fold_meta_preds_reg_scaled.reshape(-1, 1)).flatten()
-
-        # Calculate Regression metrics on scaled data
-        rmse = root_mean_squared_error(y_test_reg_scaled, fold_meta_preds_reg_scaled)
-        mape = mean_absolute_percentage_error_custom(y_test_reg_scaled, fold_meta_preds_reg_scaled)
+        # The meta-model predicts the fold-standardized target. Use this fold's
+        # training-fitted scaler exactly once, then evaluate against raw prices.
+        fold_meta_preds_reg, rmse, mape = regression_metrics_original_price(
+            y_test_reg_fold,
+            fold_meta_preds_reg_scaled,
+            prediction_scaler=scaler_y_reg_eval,
+        )
 
         # Store Regression metrics
         rmses.append(rmse)
         mapes.append(mape)
 
         # Print and log Regression metrics
-        print(f"Fold {fold + 1} - Regression: RMSE={rmse:.4f}, MAPE={mape:.4f}")
-        logging.info(f"Fold {fold + 1} - Regression: RMSE={rmse:.4f}, MAPE={mape:.4f}")
+        print(f"Fold {fold + 1} - Regression: RMSE (USD/oz)={rmse:.4f}, MAPE (%)={mape:.4f}")
+        logging.info(f"Fold {fold + 1} - Regression: RMSE (USD/oz)={rmse:.4f}, MAPE (%)={mape:.4f}")
 
         # Train base models for Classification
         models_clf, optimizers_clf, schedulers_clf, early_stoppings_clf, focal_loss = train_base_models_clf(
@@ -1219,20 +1222,20 @@
 
         # Predict with Meta-Model for Regression
         fold_meta_preds_reg_scaled = meta_model_reg.predict(fold_meta_features_reg)
-        # Inverse transform the predictions to original scale (if needed)
-        # fold_meta_preds_reg = scaler_y_reg_eval.inverse_transform(fold_meta_preds_reg_scaled.reshape(-1, 1)).flatten()
-
-        # Calculate Regression metrics on scaled data
-        rmse = root_mean_squared_error(y_test_reg_scaled, fold_meta_preds_reg_scaled)
-        mape = mean_absolute_percentage_error_custom(y_test_reg_scaled, fold_meta_preds_reg_scaled)
+        # Restore this fold's standardized predictions to original prices.
+        fold_meta_preds_reg, rmse, mape = regression_metrics_original_price(
+            y_test_reg_fold,
+            fold_meta_preds_reg_scaled,
+            prediction_scaler=scaler_y_reg_eval,
+        )
 
         # Store Regression metrics
         rmses.append(rmse)
         mapes.append(mape)
 
         # Print and log Regression metrics
-        print(f"Fold {fold + 1} - Regression: RMSE={rmse:.4f}, MAPE={mape:.4f}")
-        logging.info(f"Fold {fold + 1} - Regression: RMSE={rmse:.4f}, MAPE={mape:.4f}")
+        print(f"Fold {fold + 1} - Regression: RMSE (USD/oz)={rmse:.4f}, MAPE (%)={mape:.4f}")
+        logging.info(f"Fold {fold + 1} - Regression: RMSE (USD/oz)={rmse:.4f}, MAPE (%)={mape:.4f}")
 
         # Train base models for Classification
         models_clf, optimizers_clf, schedulers_clf, early_stoppings_clf, focal_loss = train_base_models_clf(
@@ -1421,20 +1424,20 @@
 
         # Predict with Meta-Model for Regression
         fold_meta_preds_reg_scaled = meta_model_reg.predict(fold_meta_features_reg)
-        # Inverse transform the predictions to original scale (if needed)
-        # fold_meta_preds_reg = scaler_y_reg_eval.inverse_transform(fold_meta_preds_reg_scaled.reshape(-1, 1)).flatten()
-
-        # Calculate Regression metrics on scaled data
-        rmse = root_mean_squared_error(y_test_reg_scaled, fold_meta_preds_reg_scaled)
-        mape = mean_absolute_percentage_error_custom(y_test_reg_scaled, fold_meta_preds_reg_scaled)
+        # Restore this fold's standardized predictions to original prices.
+        fold_meta_preds_reg, rmse, mape = regression_metrics_original_price(
+            y_test_reg_fold,
+            fold_meta_preds_reg_scaled,
+            prediction_scaler=scaler_y_reg_eval,
+        )
 
         # Store Regression metrics
         rmses.append(rmse)
         mapes.append(mape)
 
         # Print and log Regression metrics
-        print(f"Fold {fold + 1} - Regression: RMSE={rmse:.4f}, MAPE={mape:.4f}")
-        logging.info(f"Fold {fold + 1} - Regression: RMSE={rmse:.4f}, MAPE={mape:.4f}")
+        print(f"Fold {fold + 1} - Regression: RMSE (USD/oz)={rmse:.4f}, MAPE (%)={mape:.4f}")
+        logging.info(f"Fold {fold + 1} - Regression: RMSE (USD/oz)={rmse:.4f}, MAPE (%)={mape:.4f}")
 
         # Train base models for Classification
         models_clf, optimizers_clf, schedulers_clf, early_stoppings_clf, focal_loss = train_base_models_clf(
@@ -1623,20 +1626,20 @@
 
         # Predict with Meta-Model for Regression
         fold_meta_preds_reg_scaled = meta_model_reg.predict(fold_meta_features_reg)
-        # Inverse transform the predictions to original scale (if needed)
-        # fold_meta_preds_reg = scaler_y_reg_eval.inverse_transform(fold_meta_preds_reg_scaled.reshape(-1, 1)).flatten()
-
-        # Calculate Regression metrics on scaled data
-        rmse = root_mean_squared_error(y_test_reg_scaled, fold_meta_preds_reg_scaled)
-        mape = mean_absolute_percentage_error_custom(y_test_reg_scaled, fold_meta_preds_reg_scaled)
+        # Restore this fold's standardized predictions to original prices.
+        fold_meta_preds_reg, rmse, mape = regression_metrics_original_price(
+            y_test_reg_fold,
+            fold_meta_preds_reg_scaled,
+            prediction_scaler=scaler_y_reg_eval,
+        )
 
         # Store Regression metrics
         rmses.append(rmse)
         mapes.append(mape)
 
         # Print and log Regression metrics
-        print(f"Fold {fold + 1} - Regression: RMSE={rmse:.4f}, MAPE={mape:.4f}")
-        logging.info(f"Fold {fold + 1} - Regression: RMSE={rmse:.4f}, MAPE={mape:.4f}")
+        print(f"Fold {fold + 1} - Regression: RMSE (USD/oz)={rmse:.4f}, MAPE (%)={mape:.4f}")
+        logging.info(f"Fold {fold + 1} - Regression: RMSE (USD/oz)={rmse:.4f}, MAPE (%)={mape:.4f}")
 
         # Train base models for Classification
         models_clf, optimizers_clf, schedulers_clf, early_stoppings_clf, focal_loss = train_base_models_clf(
@@ -1825,20 +1828,20 @@
 
         # Predict with Meta-Model for Regression
         fold_meta_preds_reg_scaled = meta_model_reg.predict(fold_meta_features_reg)
-        # Inverse transform the predictions to original scale (if needed)
-        # fold_meta_preds_reg = scaler_y_reg_eval.inverse_transform(fold_meta_preds_reg_scaled.reshape(-1, 1)).flatten()
-
-        # Calculate Regression metrics on scaled data
-        rmse = root_mean_squared_error(y_test_reg_scaled, fold_meta_preds_reg_scaled)
-        mape = mean_absolute_percentage_error_custom(y_test_reg_scaled, fold_meta_preds_reg_scaled)
+        # Restore this fold's standardized predictions to original prices.
+        fold_meta_preds_reg, rmse, mape = regression_metrics_original_price(
+            y_test_reg_fold,
+            fold_meta_preds_reg_scaled,
+            prediction_scaler=scaler_y_reg_eval,
+        )
 
         # Store Regression metrics
         rmses.append(rmse)
         mapes.append(mape)
 
         # Print and log Regression metrics
-        print(f"Fold {fold + 1} - Regression: RMSE={rmse:.4f}, MAPE={mape:.4f}")
-        logging.info(f"Fold {fold + 1} - Regression: RMSE={rmse:.4f}, MAPE={mape:.4f}")
+        print(f"Fold {fold + 1} - Regression: RMSE (USD/oz)={rmse:.4f}, MAPE (%)={mape:.4f}")
+        logging.info(f"Fold {fold + 1} - Regression: RMSE (USD/oz)={rmse:.4f}, MAPE (%)={mape:.4f}")
 
         # Train base models for Classification
         models_clf, optimizers_clf, schedulers_clf, early_stoppings_clf, focal_loss = train_base_models_clf(
@@ -1932,12 +1935,12 @@
 
     # Evaluate Metrics
     print("\nOverall Regression Results:")
-    print(f"Average RMSE: {np.mean(rmses):.4f} ± {np.std(rmses):.4f}")
-    print(f"Average MAPE: {np.mean(mapes):.4f} ± {np.std(mapes):.4f}")
+    print(f"Average RMSE (USD/oz): {np.mean(rmses):.4f} ± {np.std(rmses):.4f}")
+    print(f"Average MAPE (%): {np.mean(mapes):.4f} ± {np.std(mapes):.4f}")
     logging.info(
         f"Overall Regression Results: "
-        f"Average RMSE={np.mean(rmses):.4f} ± {np.std(rmses):.4f}, "
-        f"Average MAPE={np.mean(mapes):.4f} ± {np.std(mapes):.4f}"
+        f"Average RMSE (USD/oz)={np.mean(rmses):.4f} ± {np.std(rmses):.4f}, "
+        f"Average MAPE (%)={np.mean(mapes):.4f} ± {np.std(mapes):.4f}"
     )
 
     print("\nOverall Classification Results:")
@@ -1958,8 +1961,8 @@
     # Save Overall Results
     with open('evaluation_results.txt', 'w') as f:
         f.write("Overall Regression Results:\n")
-        f.write(f"Average RMSE: {np.mean(rmses):.4f} ± {np.std(rmses):.4f}\n")
-        f.write(f"Average MAPE: {np.mean(mapes):.4f} ± {np.std(mapes):.4f}\n\n")
+        f.write(f"Average RMSE (USD/oz): {np.mean(rmses):.4f} ± {np.std(rmses):.4f}\n")
+        f.write(f"Average MAPE (%): {np.mean(mapes):.4f} ± {np.std(mapes):.4f}\n\n")
         f.write("Overall Classification Results:\n")
         f.write(f"Average Accuracy: {np.mean(accuracies):.4f} ± {np.std(accuracies):.4f}\n")
         f.write(f"Average Precision: {np.mean(precisions):.4f} ± {np.std(precisions):.4f}\n")
--- before/RF.py
+++ after/RF.py
@@ -5,10 +5,12 @@
 from sklearn.preprocessing import StandardScaler
 from sklearn.metrics import (
     accuracy_score, precision_score, recall_score, f1_score, roc_auc_score,
-    mean_squared_error, mean_absolute_percentage_error
+    mean_squared_error
 )
+from regression_scale_utils import regression_metrics_original_price
 import warnings
 import ta  # 技术指标库
+from pathlib import Path
 
 warnings.filterwarnings('ignore')  # Suppress warnings
 
@@ -134,21 +136,23 @@
     auc = roc_auc_score(y_cls_test, y_cls_prob)
 
     # 回归指标
-    rmse = np.sqrt(mean_squared_error(y_reg_test, y_reg_pred))
-    mape = mean_absolute_percentage_error(y_reg_test, y_reg_pred)
-
-    return acc, prec, rec, f1, auc, rmse, mape
+    # y was never standardized: evaluate the original-price prediction directly.
+    _, rmse, mape = regression_metrics_original_price(y_reg_test, y_reg_pred)
+
+    return acc, prec, rec, f1, auc, rmse, mape, y_reg_pred
 
 
 def main():
-    file_path = '/home/w/桌面/lilj/GOLD_cleaned.xlsx'
+    file_path = Path(__file__).resolve().parent / 'GOLD_cleaned.xlsx'
     X, y_cls, y_reg = preprocess_data(file_path)
 
     tscv = TimeSeriesSplit(n_splits=5)
 
     results = {
-        "accuracy": [], "precision": [], "recall": [], "f1": [], "auc": [], "rmse": [], "mape": []
+        "accuracy": [], "precision": [], "recall": [], "f1": [], "auc": [],
+        "rmse_usd_per_oz": [], "mape_percent": []
     }
+    regression_predictions = []
 
     for fold, (train_index, test_index) in enumerate(tscv.split(X)):
         print(f"Fold {fold + 1}")
@@ -161,7 +165,7 @@
         X_train_scaled = scaler.fit_transform(X_train)
         X_test_scaled = scaler.transform(X_test)
 
-        acc, prec, rec, f1, auc, rmse, mape = train_random_forest(
+        acc, prec, rec, f1, auc, rmse, mape, y_reg_pred = train_random_forest(
             X_train_scaled, y_cls_train, y_reg_train, X_test_scaled, y_cls_test, y_reg_test
         )
 
@@ -170,15 +174,29 @@
         results["recall"].append(rec)
         results["f1"].append(f1)
         results["auc"].append(auc)
-        results["rmse"].append(rmse)
-        results["mape"].append(mape)
+        results["rmse_usd_per_oz"].append(rmse)
+        results["mape_percent"].append(mape)
+        regression_predictions.append(pd.DataFrame({
+            "fold": fold + 1,
+            "row_index": X_test.index.to_numpy(),
+            "y_true_price_original": np.asarray(y_reg_test),
+            "y_pred_price_original": np.asarray(y_reg_pred),
+        }))
 
         print(f"Fold {fold + 1} - Accuracy: {acc:.4f}, Precision: {prec:.4f}, Recall: {rec:.4f}, F1: {f1:.4f}, AUC: {auc:.4f}")
-        print(f"Fold {fold + 1} - RMSE: {rmse:.4f}, MAPE: {mape:.4f}")
+        print(f"Fold {fold + 1} - RMSE (USD/oz): {rmse:.4f}, MAPE (%): {mape:.4f}")
 
     print("\nOverall Results:")
+    labels = {"rmse_usd_per_oz": "RMSE (USD/oz)", "mape_percent": "MAPE (%)"}
     for metric in results:
-        print(f"{metric.capitalize()}: {np.mean(results[metric]):.4f} ± {np.std(results[metric]):.4f}")
+        print(f"{labels.get(metric, metric.capitalize())}: {np.mean(results[metric]):.4f} ± {np.std(results[metric]):.4f}")
+    pd.concat(regression_predictions, ignore_index=True).to_csv(
+        "RF_regression_predictions_original_price.csv", index=False, encoding="utf-8-sig")
+    pd.DataFrame({
+        "metric": [labels.get(metric, metric) for metric in results],
+        "mean": [np.mean(results[metric]) for metric in results],
+        "std": [np.std(results[metric]) for metric in results],
+    }).to_csv("RF_metric_summary.csv", index=False, encoding="utf-8-sig")
 
 if __name__ == "__main__":
     main()
--- before/XGBoost.py
+++ after/XGBoost.py
@@ -5,10 +5,12 @@
 from sklearn.preprocessing import StandardScaler
 from sklearn.metrics import (
     accuracy_score, precision_score, recall_score, f1_score, roc_auc_score,
-    mean_squared_error, mean_absolute_percentage_error
+    mean_squared_error
 )
+from regression_scale_utils import regression_metrics_original_price
 import warnings
 import ta  # 技术指标库
+from pathlib import Path
 
 warnings.filterwarnings('ignore')  # Suppress warnings
 
@@ -144,21 +146,23 @@
     auc = roc_auc_score(y_cls_test, y_cls_prob)
 
     # 回归指标
-    rmse = np.sqrt(mean_squared_error(y_reg_test, y_reg_pred))
-    mape = mean_absolute_percentage_error(y_reg_test, y_reg_pred)
-
-    return acc, prec, rec, f1, auc, rmse, mape
+    # y was never standardized: evaluate the original-price prediction directly.
+    _, rmse, mape = regression_metrics_original_price(y_reg_test, y_reg_pred)
+
+    return acc, prec, rec, f1, auc, rmse, mape, y_reg_pred
 
 
 def main():
-    file_path = '/home/w/桌面/lilj/GOLD_cleaned.xlsx'
+    file_path = Path(__file__).resolve().parent / 'GOLD_cleaned.xlsx'
     X, y_cls, y_reg = preprocess_data(file_path)
 
     tscv = TimeSeriesSplit(n_splits=5)
 
     results = {
-        "accuracy": [], "precision": [], "recall": [], "f1": [], "auc": [], "rmse": [], "mape": []
+        "accuracy": [], "precision": [], "recall": [], "f1": [], "auc": [],
+        "rmse_usd_per_oz": [], "mape_percent": []
     }
+    regression_predictions = []
 
     for fold, (train_index, test_index) in enumerate(tscv.split(X)):
         print(f"Fold {fold + 1}")
@@ -171,7 +175,7 @@
         X_train_scaled = scaler.fit_transform(X_train)
         X_test_scaled = scaler.transform(X_test)
 
-        acc, prec, rec, f1, auc, rmse, mape = train_xgboost(
+        acc, prec, rec, f1, auc, rmse, mape, y_reg_pred = train_xgboost(
             X_train_scaled, y_cls_train, y_reg_train, X_test_scaled, y_cls_test, y_reg_test
         )
 
@@ -180,15 +184,29 @@
         results["recall"].append(rec)
         results["f1"].append(f1)
         results["auc"].append(auc)
-        results["rmse"].append(rmse)
-        results["mape"].append(mape)
+        results["rmse_usd_per_oz"].append(rmse)
+        results["mape_percent"].append(mape)
+        regression_predictions.append(pd.DataFrame({
+            "fold": fold + 1,
+            "row_index": X_test.index.to_numpy(),
+            "y_true_price_original": np.asarray(y_reg_test),
+            "y_pred_price_original": np.asarray(y_reg_pred),
+        }))
 
         print(f"Fold {fold + 1} - Accuracy: {acc:.4f}, Precision: {prec:.4f}, Recall: {rec:.4f}, F1: {f1:.4f}, AUC: {auc:.4f}")
-        print(f"Fold {fold + 1} - RMSE: {rmse:.4f}, MAPE: {mape:.4f}")
+        print(f"Fold {fold + 1} - RMSE (USD/oz): {rmse:.4f}, MAPE (%): {mape:.4f}")
 
     print("\nOverall Results:")
+    labels = {"rmse_usd_per_oz": "RMSE (USD/oz)", "mape_percent": "MAPE (%)"}
     for metric in results:
-        print(f"{metric.capitalize()}: {np.mean(results[metric]):.4f} ± {np.std(results[metric]):.4f}")
+        print(f"{labels.get(metric, metric.capitalize())}: {np.mean(results[metric]):.4f} ± {np.std(results[metric]):.4f}")
+    pd.concat(regression_predictions, ignore_index=True).to_csv(
+        "XGBoost_regression_predictions_original_price.csv", index=False, encoding="utf-8-sig")
+    pd.DataFrame({
+        "metric": [labels.get(metric, metric) for metric in results],
+        "mean": [np.mean(results[metric]) for metric in results],
+        "std": [np.std(results[metric]) for metric in results],
+    }).to_csv("XGBoost_metric_summary.csv", index=False, encoding="utf-8-sig")
 
 if __name__ == "__main__":
     main()
--- before/gold lightgbm.py
+++ after/gold lightgbm.py
@@ -5,10 +5,12 @@
 from sklearn.preprocessing import StandardScaler
 from sklearn.metrics import (
     accuracy_score, precision_score, recall_score, f1_score, roc_auc_score,
-    mean_squared_error, mean_absolute_percentage_error
+    mean_squared_error
 )
+from regression_scale_utils import regression_metrics_original_price
 import warnings
 import ta  # 技术指标库
+from pathlib import Path
 
 warnings.filterwarnings('ignore')  # Suppress warnings
 
@@ -140,21 +142,23 @@
     auc = roc_auc_score(y_cls_test, y_cls_prob)
 
     # 回归指标
-    rmse = np.sqrt(mean_squared_error(y_reg_test, y_reg_pred))
-    mape = mean_absolute_percentage_error(y_reg_test, y_reg_pred)
-
-    return acc, prec, rec, f1, auc, rmse, mape
+    # y was never standardized: evaluate the original-price prediction directly.
+    _, rmse, mape = regression_metrics_original_price(y_reg_test, y_reg_pred)
+
+    return acc, prec, rec, f1, auc, rmse, mape, y_reg_pred
 
 
 def main():
-    file_path = '/home/w/桌面/lilj/GOLD_cleaned.xlsx'
+    file_path = Path(__file__).resolve().parent / 'GOLD_cleaned.xlsx'
     X, y_cls, y_reg = preprocess_data(file_path)
 
     tscv = TimeSeriesSplit(n_splits=5)
 
     results = {
-        "accuracy": [], "precision": [], "recall": [], "f1": [], "auc": [], "rmse": [], "mape": []
+        "accuracy": [], "precision": [], "recall": [], "f1": [], "auc": [],
+        "rmse_usd_per_oz": [], "mape_percent": []
     }
+    regression_predictions = []
 
     for fold, (train_index, test_index) in enumerate(tscv.split(X)):
         print(f"Fold {fold + 1}")
@@ -167,7 +171,7 @@
         X_train_scaled = scaler.fit_transform(X_train)
         X_test_scaled = scaler.transform(X_test)
 
-        acc, prec, rec, f1, auc, rmse, mape = train_lightgbm(
+        acc, prec, rec, f1, auc, rmse, mape, y_reg_pred = train_lightgbm(
             X_train_scaled, y_cls_train, y_reg_train, X_test_scaled, y_cls_test, y_reg_test
         )
 
@@ -176,15 +180,29 @@
         results["recall"].append(rec)
         results["f1"].append(f1)
         results["auc"].append(auc)
-        results["rmse"].append(rmse)
-        results["mape"].append(mape)
+        results["rmse_usd_per_oz"].append(rmse)
+        results["mape_percent"].append(mape)
+        regression_predictions.append(pd.DataFrame({
+            "fold": fold + 1,
+            "row_index": X_test.index.to_numpy(),
+            "y_true_price_original": np.asarray(y_reg_test),
+            "y_pred_price_original": np.asarray(y_reg_pred),
+        }))
 
         print(f"Fold {fold + 1} - Accuracy: {acc:.4f}, Precision: {prec:.4f}, Recall: {rec:.4f}, F1: {f1:.4f}, AUC: {auc:.4f}")
-        print(f"Fold {fold + 1} - RMSE: {rmse:.4f}, MAPE: {mape:.4f}")
+        print(f"Fold {fold + 1} - RMSE (USD/oz): {rmse:.4f}, MAPE (%): {mape:.4f}")
 
     print("\nOverall Results:")
+    labels = {"rmse_usd_per_oz": "RMSE (USD/oz)", "mape_percent": "MAPE (%)"}
     for metric in results:
-        print(f"{metric.capitalize()}: {np.mean(results[metric]):.4f} ± {np.std(results[metric]):.4f}")
+        print(f"{labels.get(metric, metric.capitalize())}: {np.mean(results[metric]):.4f} ± {np.std(results[metric]):.4f}")
+    pd.concat(regression_predictions, ignore_index=True).to_csv(
+        "LightGBM_regression_predictions_original_price.csv", index=False, encoding="utf-8-sig")
+    pd.DataFrame({
+        "metric": [labels.get(metric, metric) for metric in results],
+        "mean": [np.mean(results[metric]) for metric in results],
+        "std": [np.std(results[metric]) for metric in results],
+    }).to_csv("LightGBM_metric_summary.csv", index=False, encoding="utf-8-sig")
 
 if __name__ == "__main__":
     main()
