# 零 FCNN 运行逻辑核对

结论：**新版本与目前保留的旧零 FCNN 运行器在两阶段五折 stacking 主流程上相同，但不是完全相同的运行配置。最重要的差异是 Optuna sampler seed。** 本轮只核对并记录，没有修改模型代码或参数。

## 对照对象

- 新版本：当前工作区的 `no_fcnn_model.py` 和 `run_no_fcnn.py`。
- 旧零 FCNN 版本：参考项目 `C:/Users/WKU-MATH-558/Desktop/gold1` 中保留的 `no_fcnn_model.py` 及其相邻运行器的零 FCNN 分支。
- 原始完整模型：当前 `归一.py` 及保留的原始论文参数源快照，用于核对预处理和搜索定义。

核对没有导入或执行旧运行器的分析功能。仅在独立核对进程中选取预处理、折准备、模型构造和训练入口函数进行比较；本地模型没有新增历史代码依赖，也没有加入统计检验代码。

## 与旧零 FCNN 运行器一致的部分

| 环节 | 旧零 FCNN 运行器 | 新入口 | 验证 |
| --- | --- | --- | --- |
| 输入与特征 | 4,419 条清洗样本、48 个特征 | 相同 | 真实数据逐元素一致 |
| 分类标签 | `(GOLD.diff() > 0).astype(int)` | 相同 | 真实数据逐项一致 |
| 回归标签 | `GOLD.shift(-1)` | 相同 | 真实数据逐项一致 |
| 时间划分 | `TimeSeriesSplit(n_splits=5)` | 相同 | 训练/测试位置和日期边界相同 |
| X 缩放 | 回归/分类分别用训练折拟合 StandardScaler | 相同 | 五折所有输入数组完全相同 |
| y 缩放 | 训练折拟合，测试折仅 transform | 相同 | 五折均值、尺度和目标数组完全相同 |
| 基模型顺序 | LightGBM、XGBoost、Random Forest | 相同 | seed=42 且给定相同最优 LightGBM 参数时，六个模型类和全部构造参数一致 |
| 元特征 | 回归 3 列、分类两类概率合计 6 列 | 相同 | 顺序与维度相同 |
| 第一阶段 | 五折训练基模型，收集测试折元特征与标签 | 相同 | 入口执行轨迹一致 |
| GBM | 汇合五折数据后拟合；300 stages、lr=0.15、depth=5、seed=42 | 相同 | 受控对照使用真实 GBM 拟合，结果完全一致 |
| 第二阶段 | 同五折重新优化并训练基模型，用已训练 GBM 预测 | 相同 | 总计均为 10 次训练折调用 |
| Optuna 预算 | 每任务/折/阶段 50 trials，目标内部 cv=3 | 相同 | 完整运行共 1,000 trials，未缩减 |
| 原价评估 | 当前训练折 y scaler 逆变换预测一次；真实值原价 | 相同 | 原价预测、RMSE 和百分数 MAPE 一致 |
| 历史泄漏 | 旧分类标签；元模型已使用最终评估区间标签 | 相同 | 未将其更改为无泄漏协议 |

五折样本数及日期：

| 折 | 训练条数 | 测试条数 | 测试开始 | 测试结束 |
| --- | --- | --- | --- | --- |
| 1 | 739 | 736 | 2006-01-19 | 2009-08-27 |
| 2 | 1,475 | 736 | 2009-08-28 | 2013-05-28 |
| 3 | 2,211 | 736 | 2013-05-29 | 2017-05-22 |
| 4 | 2,947 | 736 | 2017-05-23 | 2021-01-15 |
| 5 | 3,683 | 736 | 2021-01-19 | 2024-08-29 |

## 实际差异及影响

1. **Optuna sampler seed 不同，会影响训练结果。** 保留的旧运行器在加载原始源代码时，将 `create_study(...)` 改写为 `create_study(..., sampler=TPESampler(seed=seed))`，默认 seed=42。新模型直接保留 `归一.py` 的 `create_study(direction=...)`，没有指定 sampler seed。NumPy seed=42 不能替代 Optuna sampler 自己的种子；两者可能搜索出不同 LightGBM 参数，进而产生不同预测。因此，新模型与原始源快照的搜索定义一致，但不能称为与保留旧运行器的有效搜索配置完全一致。

2. **随机种子入口不同。** 旧运行器支持 `--seed`，会同步改写模型和相关随机源；新入口固定模型、NumPy 和 Python random 为 42。与旧默认 seed=42 的模型参数一致，不能代表其他 seed 的运行。新模型不使用 PyTorch，因而不设置其随机源。

3. **输入防护有所减少。** 旧运行器检查对齐日期的长度、空值和重复日期，主入口还做源文件哈希与两种结构的预处理对齐检查。新入口没有完整复制这些检查，尤其没有显式拒绝重复日期。本次使用的数据日期有效且唯一，因此没有改变此次折划分；换数据时，两者失败条件可能不同。

4. **日志和结果文件不完全相同。** 新入口省略了每个树模型的训练集 MSE/log-loss、标准化尺度 native 诊断指标和 MAE；保留常规分类指标、原价 RMSE 和百分数 MAPE，并增加五折均值/标准差汇总。CSV 原价真实值列由旧 `y_true_price` 改为 `y_true_price_original`，新增 `row_index`，逐条 CSV 不再带旧 `seed`、`run_id` 列。这些字段变化不参与训练，但旧读取脚本不能假定格式完全不变。

5. **分析步骤按要求移除。** 新入口只训练和评估本地零 FCNN 模型，不启动完整模型对照或统计检验。这是用户要求的功能范围变化。

上述 sampler 对比针对**目前磁盘保留的旧运行器**。2026-09-14 的 `AUDIT.json` 未记录 sampler seed；原始源快照也没有显式 sampler seed。因此不能仅凭后来保留的运行器，反推当日已完成运行的全部有效随机配置。

## 与 `归一.py` 原始 main 的区别

`归一.py` 的主函数包含一次 OOF 阶段及 **5 次重复的五折评估阶段**，回归元模型累计 fit 6 次；新零 FCNN 入口只有一次 OOF、一次五折评估和一次元模型 fit。这与保留的旧零 FCNN 运行器相同，**不是将 `归一.py` 的完整主函数只删掉两个 FCNN 后逐段照搬**。

所以，“运行逻辑一致”需要限定为：与旧零 FCNN 分支的两阶段五折主流程一致。不能泛化为与 `归一.py` 所有重复执行段及耗时预算完全一致。

## 实测范围

- 用实际 GOLD 工作簿逐项比较新旧预处理、五折输入、y scaler 及保存的历史日期边界。
- 给定同一组 LightGBM 最优参数，比较新旧六个基模型的全部构造参数。
- 让新旧实际入口接收相同的模拟基模型预测，分别训练真实的 300-stage GBM：10 次训练折调用轨迹相同，50 条合成最终预测的 9 个共同字段及全部共同合并指标逐项完全一致。
- 没有进行耗时的完整 Optuna 搜索或 GOLD 模型重训；因此没有声称两种 sampler 配置下的正式训练预测相同。

机器可读证据及对照源码 SHA256 见 `no_fcnn_logic_verification.json`。
