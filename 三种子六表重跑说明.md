# 三种子六表重跑

运行前的可回退状态是远程标签 `pre-dm-tables-20260918`。DM 检验代码已在提交 `f050519` 推送；本次统一运行入口须先推送，之后才启动训练。完整的跨设备回退命令见 `DM检验与跨设备回退说明.md`。

数据固定为 `GOLD_cleaned.xlsx`，清洗后 4,419 行，TimeSeriesSplit 五折，每折测试 736 行。三个随机种子为 42、43、44。分类标签仍是 `(GOLD.diff()>0)`，回归标签是 `GOLD.shift(-1)`；已知的分类标签及元模型评估泄漏仍然存在。

每个种子运行八种模型。完整 BFNE 和零 FCNN 使用一次五折 OOF 元特征生成及五轮评估；Table 8、9 和 DM 仅使用第一轮每个日期唯一一次的预测。without FE 是只用论文 Table 1 十一个原始变量的重建完整模型，使用一次 OOF 和一次评估。Random Walk 是新写的 GOLD_t 持久性基线；CNN-LSTM 分类使用对齐后的原脚本，价格回归头是新增重建。后三种不能称为找到的作者原实现。独立树模型使用原脚本的模型结构、超参数和原价回归目标；三种子改变随机状态。Optuna 保留 50 次搜索和三折目标函数，加入确定性 sampler 随机种子；原目标函数内部模型随机状态仍为 42，最终模型随实验种子变化。神经网络 epoch、折边界、输入、标签、stacking 结构保持原样。

先在 `seeded_runs/seed42` 等目录下逐模型运行。各入口的命令格式：

```powershell
python run_seeded_ensembles.py --model full --seed 42 --output seeded_runs/seed42/full
python run_seeded_ensembles.py --model no_fcnn --seed 42 --output seeded_runs/seed42/no_fcnn
python run_seeded_ensembles.py --model without_fe --seed 42 --output seeded_runs/seed42/without_fe
python run_seeded_baselines.py --model random_forest --seed 42 --output seeded_runs/seed42/random_forest
python run_seeded_baselines.py --model xgboost --seed 42 --output seeded_runs/seed42/xgboost
python run_seeded_baselines.py --model lightgbm --seed 42 --output seeded_runs/seed42/lightgbm
python run_seeded_baselines.py --model random_walk --seed 42 --output seeded_runs/seed42/random_walk
python reconstructed_cnn_lstm.py --seed 42 --task both --output seeded_runs/seed42/cnn_lstm
```

对 43、44 重复同样命令。训练结果逐折保存，重复相同命令可跳过已完成折；CNN-LSTM 入口尚不支持逐折续跑，故该模型的中断需要重新运行。所有 24 个模型运行状态均为 `completed` 后执行：

```powershell
python build_six_tables.py --runs seeded_runs --output 六表_新重跑_42_43_44
```

也可以执行 `python run_all_seeded.py` 顺序跑完缺少的模型、生成六表并自动提交推送；若已有 seed 42 完整模型进程正在运行，则使用 `--wait-for-full42` 等待它完成后继续。自动入口一旦有模型失败就停止，不会发布不完整结果。

输出六张 CSV 和一个六工作表 Excel。前 3 张是 seed 42 的 Table 8、Table 9、完整模型对零 FCNN 的 DM；后 3 张是 42、43、44 五折指标的平均 Table 8、Table 9，以及先按日期平均三种子的配对损失再做一次 DM。RMSE 单位 USD/oz，MAPE 单位百分数；回归标准化模型只使用对应训练折的 scaler 逆变换预测一次，真实值始终为原始价格。DM 不会对三个 p 值直接求平均。生成器不覆盖历史表；结果需要另行提交推送。
