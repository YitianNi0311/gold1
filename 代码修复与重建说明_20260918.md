# 历史 seed 42 代码修复与重建说明

本轮只修复代码，没有重训模型、生成新表格分数或改写 `Table8_9_10_同次实验_seed42.xlsx`。`545/guiyi.py` 可用于核对分类最终评估，但 `545` 中没有找到 Random Walk、价格回归 CNN-LSTM 或完整 without FE 的作者实现。

## 已修复的现有模型

- `归一.py`：第一轮最终评估五折现已计算并汇总分类 Accuracy、Precision、Recall、F1、AUC；后四轮仍只产生回归指标。回归继续使用对应折 y scaler 逆变换一次，真实值为原价。输入工作簿从脚本所在目录读取。
- `去2FCNN.py`、`去掉2个.py`：训练仍用原有标准化目标；最终评估改为同折 scaler 将预测恢复到 USD/oz 后，与原价真值比较。它们仍各含一个 FCNN，不能作为零 FCNN。
- `去no1fcnn.py`、`加上软投票.py`、`最后的尝试.py`、`feature_analysis.py`、`不拆分.py`、`FCNN1.py`、`FCNN2.py`、`RandomFO.py`：原价回归输出中的 MAPE 改为百分数，乘 100 一次；指标输出增加单位。`ANNgold.py` 只将原有 RMSE 标为 USD/oz，保留其单独的 MASE 指标。
- `RF.py`、`XGBoost.py`、`gold lightgbm.py` 原有原价评估已符合要求，未更改模型。

## 新增模型代码及来源

| 入口 | 性质 | 运行时行为 |
| --- | --- | --- |
| `run_no_fcnn.py` + `no_fcnn_model.py` | 独立零 FCNN：LightGBM、XGBoost、Random Forest → GBM | 保留原参数和每次 Optuna 预算；一次五折 OOF、五轮五折回归评估、第一轮五折分类评估。新运行目录保存逐条预测、逐折指标及同折 y scaler。 |
| `reconstructed_random_walk.py` | 新建持久性基线，未找到作者原实现 | 以当日 GOLD 预测下一交易日 GOLD；只在通过历史日期、标签和价格对齐后写入独立目录。 |
| `reconstructed_cnn_lstm.py` | 分类使用已有 `lstm/CNN-LSTM_tuned.py` 并对齐 4,419 样本；价格回归头为新建 | 分类继续采用该 tuned 版本的内层学习率选择及五折结构；回归复用其 CNN/LSTM 主体，新增单输出头，用各训练折拟合的 y scaler，原价评估。它不是已找到的作者回归实现。 |
| `reconstructed_without_fe.py` | 按论文定义重建，未找到作者原实现 | 只取论文 Table 1 的 11 个原始宏观金融输入，删除完整模型的另外 37 个工程特征；保留 FCNN1＋FCNN2＋三树模型→GBM、原标签和五折。使用一次 OOF、一次五折评估，与这次保存的历史预测流程相同。完整运行有 10 次训练折及相应 Optuna 搜索。 |

`historical_alignment.py` 在任何重建训练前读取 `seed42_reference_targets.csv`，逐行核对折号、测试日期、真实分类标签和原始金价；必须同时满足 4,419 条清洗样本、48 个原完整模型特征、3,680 条测试预测、每折 736 条。仅预测条数相同不算通过。该参考文件只含测试目标，不含模型预测或成绩，由历史 `full/predictions.csv` 提取；来源与两个 SHA256 记在 `seed42_reference_targets_manifest.json` 中。without FE 再断言所选输入恰好是论文 Table 1 的 11 列，其他 37 列全部删除。

## 验证与限制

`python -m unittest -v test_regression_scale test_additional_scales test_no_fcnn test_reconstructed_alignment`：10 项通过。合成折使用两个不同 y scaler，真实值 `[100, 200]`、原价预测 `[110, 180]` 的 MAPE 均为 `10.0`。真实数据的日期、类别标签、价格及 without FE 特征核对通过。未执行耗时的完整 Optuna/FCNN/CNN-LSTM 训练，因此当前没有这些新模型的正式预测或表格分数。

历史分类标签依旧为 `(GOLD.diff() > 0)`，回归标签为 `GOLD.shift(-1)`；既有元模型评估流程使用了评估区间标签。这些历史结果仍存在标签和评估泄漏。新增代码不会将旧高分解释为无泄漏的下一日预测成绩。
