# 历史状态复核与回归尺度修复（2026-09-17）

后续已按用户要求在当前工作区新建独立的 `no_fcnn_model.py` 和 `run_no_fcnn.py`，运行不依赖历史目录；所有 Python 代码均不包含统计检验实现。当前运行方式见 `零FCNN运行说明.md`。下文保留此前历史结果的来源和复核记录。

## 范围和来源

工作区是 `C:\Users\WKU-MATH-558\Desktop\goldnew\gold1`；保留的参考项目是 `C:\Users\WKU-MATH-558\Desktop\gold1`。两者的数据文件 SHA256 完全相同。

本次将参考项目中已经保留的四个评估修复脚本及公共指标工具恢复到当前工作区，并补充验证。四个模型脚本与参考项目逐字节一致。历史目录、论文 PDF、训练快照、旧日志和结果文件均未覆盖；未读取、导入或采用 `论文下一日预测泄漏修复_20260916` 中的代码或结果。

没有重新训练模型。未改变模型结构、特征、标签、超参数、Optuna 预算、随机种子、epoch、划分或 stacking 训练流程。`归一.py` 与原始论文参数快照的全部模型类、预处理和训练函数 AST 相同；变化只涉及 MAPE 辅助函数及 main 中的评估和输出。四个脚本的逐行修改保存在 `历史状态复核_20260917/evaluation_scale.patch`。

## 统一尺度

| 代码 | 训练目标 | 最终评估 |
| --- | --- | --- |
| `归一.py` 的全部 5 个最终回归评估块 | 各训练折拟合的 y scaler 标准化目标 | 将元模型预测交给公共工具，传入当前折的 `scaler_y_reg_eval`，仅逆变换一次；真实值直接使用 `y_test_reg_fold` 原始价格 |
| `RF.py`、`XGBoost.py`、`gold lightgbm.py` | 原始 `GOLD.shift(-1)` 价格 | 不传 y scaler，不逆变换预测；直接比较原始价格 |
| 历史 `run_experiment.py` 的 full / no_fcnn 分支 | 两种结构均使用各训练折标准化目标 | `fd["yr_test"]` 为真实原价，`pred_fit` 通过同折 `fd["scaler_y"]` 逆变换一次 |

`regression_scale_utils.py` 返回原始预测、RMSE（USD/oz）和 MAPE（%）。只有指标函数内对 MAPE 比例乘 100，外层打印、存储和 CSV 均不再乘 100。真实值 `[100, 200]` 与预测 `[110, 180]` 对应 RMSE `sqrt(250)`、MAPE `10.0`。

`归一.py` 的逐折控制台、日志、整体汇总和 `evaluation_results.txt` 均标明 `RMSE (USD/oz)`、`MAPE (%)`。三个独立树模型的结果键是 `rmse_usd_per_oz` 和 `mape_percent`，汇总 CSV 指标名包含单位，预测 CSV 保存 `y_true_price_original` 和 `y_pred_price_original`，两列均按 USD/oz 解释。树模型本身没有独立 logging 配置；历史 `.log` 是重定向的控制台输出，其逐折指标已核对。其数据路径恢复为脚本旁的同一份工作簿。

训练损失和 Optuna 优化目标继续在原训练尺度计算，不能当作最终价格 RMSE。旧运行目录中的 `native_metrics.csv`、`rmse_native`、`mape_native` 仍是历史标准化尺度诊断量，未改名伪装成修复结果，也没有用于本次价格指标汇总。

USD/oz 采用本任务和论文要求的报价口径；工作簿没有单独的单位元数据。

## 论文参数和模型结构

已直接读取 `Gold_Springer_Double_Column.pdf` 第 8–11 页（第 3.7 节及表 3–7），并对照历史 `论文参数重跑与统计检验_20260914/论文参数对照.md`、原始快照和零 FCNN 构建器。PDF 相关页的提取文本保存在 `历史状态复核_20260917/paper_parameter_pages.txt`。

| 项目 | 核对结果 |
| --- | --- |
| FCNN1 / FCNN2 | `256→128→64` / `512→256→128→64`；BatchNorm、LeakyReLU、dropout=0.5 |
| FCNN 优化 | AdamW；分类 lr=0.0005，回归 lr=0.005；weight_decay=1e-5；分类 Focal gamma=2，回归 MSE |
| 训练 | batch=32、最多 300 epoch、CosineAnnealing T_max=50；原有 early stopping 分类 patience=15、回归 patience=12 保留 |
| XGBoost | 300 trees、lr=0.03、depth=7、subsample=0.9、colsample_bytree=0.9 |
| LightGBM（完整/零 FCNN 模型） | 原 Optuna 搜索范围：100–500 trees、lr 0.01–0.1、leaves 20–100、depth 3–15、行列采样 0.5–1.0；50 trials、目标内部 cv=3 保留 |
| 独立 LightGBM 基线 | 原固定参数 500 trees、lr=0.01、depth=15、leaves=100、行列采样=0.5 保留；没有替换为完整模型的 Optuna 流程 |
| Random Forest | 200 trees、depth=10、random_state=42；`max_features` 未显式设置，存在下述论文差异 |
| GBM 元模型 | 300 stages、lr=0.15、depth=5 |
| 数据划分 | `TimeSeriesSplit(n_splits=5)` 保留；共 4,419 条清洗样本、48 个特征，测试预测 3,680 条，每折 736 条 |

**论文差异：** 表 6 将特征采样规则写为 `sqrt`，历史参数对照文档又将 RF 分类和回归默认值都解释为 `sqrt`，这一解释不准确。代码中的分类器和回归器都没有显式设置该参数；本次验证环境 sklearn 1.6.1 中，分类默认 `sqrt`，回归默认 `1.0`（全部特征），`bootstrap` 都是 True。历史 Table 8 环境清单记录 sklearn 1.4.2。本次记录论文与历史实现的差异，未给回归器新增 `max_features="sqrt"`，以免改变历史模型。

完整结构是 **FCNN1 + FCNN2 + LightGBM + XGBoost + Random Forest → GBM**，回归元特征 5 维、二分类概率元特征 10 维。

此前复核的历史零 FCNN 结构来自参考项目的 `论文参数重跑与统计检验_20260914/no_fcnn_model.py`，由同目录 `run_experiment.py` 的 `key == "no_fcnn"` 分支调用：**LightGBM + XGBoost + Random Forest → GBM**，回归 3 维、分类 6 维。当前已另行创建同结构的本地独立实现。`去掉2个.py`、`去2FCNN.py` 仍创建一个 FCNN，不能充当零 FCNN。

另一个已有的版本差异：原始训练快照中的 Optuna 没有显式 sampler seed，而目前保留的 `run_experiment.py` 在加载时注入 `TPESampler(seed=seed)`，并支持多 seed。历史参数对照文档关于 sampler seed “保持原样”的说明不能完整描述这个当前运行器。本次未增删这段逻辑，也没有拿当前运行器重训后的结果替换 2026-09-14 预测。原始未固定 sampler seed 的训练不能仅凭 seed=42 承诺逐数值重现；本次复现的是保存的预测和指标。

## 历史标签与评估泄漏

分类标签保留为 `(GOLD.diff() > 0).astype(int)`，表达当日相对前日涨跌；代码注释和部分历史输出将它称为 next-day，这与实际标签不一致。回归标签仍为 `GOLD.shift(-1)`。

输入含当日黄金滚动统计。可以由 `3 * GOLD_MA_3_days - GOLD_lag1 - GOLD_lag2` 还原当日 GOLD；结合 `GOLD_lag1` 就能确定旧分类目标。本次用数据核实了该关系。这是旧目标与输入之间的标签泄漏，历史分类高分不能表述为无泄漏的下一日方向预测成绩。

历史 stacking 先收集全部五折 OOF 特征及标签来拟合元模型，随后在同样的五个测试区间评估。五个评估折各自的 736 个标签都已经用于元模型训练；早期评估日期也受到后续折信息影响。基模型的时间划分不会消除元模型的训练/评估标签重叠。回归尺度修复仅修复单位，没有消除这项评估泄漏。

`归一.py` 原有 5 组重复评估循环和跨折标准化目标混合训练仍保留，不能将它们解释为 25 个独立测试折。其原有分类汇总列表未填充问题也未借此修改。历史有效保存的预测来自指定运行器，而非声称原始 main 已重新完成训练。

## 验证与历史指标复现

`test_regression_scale.py` 的 6 项测试通过，包括原始价格样例、两组不同 scaler、逐个执行全部 5 个实际评估代码块并计数逆变换次数、三个树模型直接接收原价目标、控制台/CSV 输出，以及历史运行器和零 FCNN 构造检查。输出验证使用模拟预测，没有训练模型。7 个本次相关 Python 文件语法检查通过。

`audit_historical_regression.py` 只读取保留数据、源文件、日志和预测，不调用训练。它验证标签、特征、日期/行号、五折边界，重建并核对 10 份训练折 scaler，重算 5 个模型的 25 个折指标，再与原日志、汇总 CSV 和 Table 8 对照。所有读取证据的 SHA256 均记录并在结束时复查。

本次复现的历史五折均值 ± 总体标准差（ddof=0）：

| 历史模型 | RMSE (USD/oz) | MAPE (%) |
| --- | --- | --- |
| BFNE-Net | 58.8121 ± 26.9759 | 3.0520 ± 1.6554 |
| BFNE-Net without FCNNs | 133.3946 ± 108.0245 | 5.1230 ± 3.4153 |
| Random Forest | 214.7165 ± 188.2713 | 13.6055 ± 13.5259 |
| XGBoost | 220.8025 ± 188.3817 | 13.8938 ± 13.6407 |
| LightGBM | 219.5146 ± 187.9192 | 14.0376 ± 13.8869 |

这些是含上述泄漏的历史协议下的结果，不是新的无泄漏预测结论。五折 RMSE 均值与合并 3,680 条样本的 RMSE 不同，分别保存，禁止混用。旧 XGBoost CSV 将 float32 预测写成短小数；按 float64 直接读回会有微小保存精度差异。验证旧日志/汇总时恢复 float32 后比较；新复核 CSV 按保存的小数直接重算，四位小数报告一致。没有对这些原价预测进行 scaler 逆变换。

结果和证据位于 `历史状态复核_20260917/`：

- `audit_results.json`：来源哈希、样本、标签、泄漏和验证状态。
- `fold_metrics.csv`、`five_fold_summary.csv`、`pooled_metrics.csv`：逐折、五折均值/标准差、合并样本三种口径。
- `fold_scaler_verification.csv`：两种结构共 10 份同折 scaler 与价格恢复核对。
- `csv_precision_verification.csv`：XGBoost CSV 保存精度差异。
- `source_manifest.json`、`evaluation_scale.patch`、`code_verification.json`：修改前后哈希、差异和训练代码不变证据。

在当前工作区 PowerShell 中复核：

```powershell
& 'C:/Users/WKU-MATH-558/miniconda3/envs/nyt/python.exe' -m unittest -v test_regression_scale test_no_fcnn
& 'C:/Users/WKU-MATH-558/miniconda3/envs/nyt/python.exe' audit_historical_regression.py --reference-root 'C:/Users/WKU-MATH-558/Desktop/gold1' --prediction-root 'C:/Users/WKU-MATH-558/Desktop/gold1/论文参数重跑与统计检验_20260914/run_20260914_114627_509349' --table-root 'C:/Users/WKU-MATH-558/Desktop/gold1/Table8重新运行_20260916_104615'
```

这组命令只验证代码和重算，不启动正式模型训练。第二条命令通过显式指定路径读取保留的预测和指标文件，不加载任何历史运行器或分析代码。新零 FCNN 训练由本地 `run_no_fcnn.py` 单独启动。
